{% extends 'base_admin.html.twig' %}

{% block title %}Editable{% endblock %}

{% block body %}
    <h1>Editable</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ editable.id }}</td>
            </tr>
            <tr>
                <th>AccessKey</th>
                <td>{{ editable.accessKey }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ editable.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_editable_index') }}">back to list</a>

    <a href="{{ path('admin_editable_edit', {'id': editable.id}) }}">edit</a>

    {{ include('admin/editable/_delete_form.html.twig') }}
{% endblock %}
