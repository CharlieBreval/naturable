{% extends 'base_admin.html.twig' %}

{% block title %}New Editable{% endblock %}

{% block body %}
    <div class="container">
        {{ include('admin/editable/_form.html.twig') }}
        <a href="{{ path('admin_editable_index') }}" class="btn btn-info">Retour à la liste</a>
    </div>


{% endblock %}
