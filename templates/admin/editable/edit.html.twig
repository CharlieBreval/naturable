{% extends 'base_admin.html.twig' %}

{% block title %}Edit Editable{% endblock %}

{% block body %}
    <div class="container">
        {{ include('admin/editable/_form.html.twig', {'button_label': 'Update'}) }}

            <a class="btn btn-primary" href="{{ path('admin_editable_index') }}">Retour à la liste</a>
            {{ include('admin/editable/_delete_form.html.twig') }}

    </div>
{% endblock %}
