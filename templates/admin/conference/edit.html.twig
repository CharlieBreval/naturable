{% extends 'base_admin.html.twig' %}

{% block title %}Edit Conference{% endblock %}

{% block body %}
    <div class="container">
    {{ include('admin/conference/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-info" href="{{ path('admin_conference_index') }}">back to list</a>

    {{ include('admin/conference/_delete_form.html.twig') }}
    </div>
{% endblock %}
