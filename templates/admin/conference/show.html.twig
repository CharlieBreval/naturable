{% extends 'base_admin.html.twig' %}

{% block title %}Conference{% endblock %}

{% block body %}
    <div class="container">
    <table class="table table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ conference.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ conference.title }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ conference.image }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ conference.content }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ conference.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_conference_index') }}">back to list</a>

    <a href="{{ path('admin_conference_edit', {'id': conference.id}) }}">edit</a>

    {{ include('admin/conference/_delete_form.html.twig') }}
    </div>
{% endblock %}
